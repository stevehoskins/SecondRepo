git init:
initialises git 
git add .:

git commit -m "a message":

git push:

git status:

git log:

git log --pretty=oneline

git branch (new_branch_) ---- note for all.. (..) just for clarity

git checkout (New_branch)

git push -u origin (new_branch)

git branch -av

git merge (new_branch)

git branch -D (new_branch)

git push origin -delete (new_branch)

etc ---- work from notes and complete this
then update webpage reference to include all of this